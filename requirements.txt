# Core fine-tuning stack
torch>=2.3
transformers>=4.44
peft>=0.12
trl>=0.9
accelerate>=0.33
bitsandbytes>=0.43        # 4-bit / QLoRA
datasets>=2.20
sentencepiece
# Optional faster training path:
# unsloth @ git+https://github.com/unslothai/unsloth.git

# Experiment tracking
wandb>=0.17
# mlflow>=2.15            # alternative backend

# Evaluation
lm-eval>=0.4.3            # lm-evaluation-harness (catastrophic forgetting suites)
rouge-score>=0.1.2
scikit-learn>=1.5
sentence-transformers>=3.0   # dedup + content-preservation similarity
openai>=1.40             # LLM-as-judge (GPT-4o)

# Inference serving
vllm>=0.5.4              # production serving; comment out if using Ollama only
fastapi>=0.112
uvicorn[standard]>=0.30
httpx>=0.27

# Utilities
pyyaml>=6.0
pydantic>=2.8
typer>=0.12
rich>=13.7
python-dotenv>=1.0

# Dev / test
pytest>=8.3
ruff>=0.6
