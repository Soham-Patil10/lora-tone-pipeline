# Catastrophic Forgetting Analysis

> Auto-generated by `src/evaluation/forgetting_check.py`.

## Method

Ran a fixed subset of standard benchmarks on the base model and the
base+adapter model with identical settings (lm-evaluation-harness).

Suites: `arc_easy`, `hellaswag`, `ifeval_subset`
Regression bound: **5.0%** relative (config `eval.forgetting.max_regression_pct`).

## Results

| suite | metric | base | fine-tuned | Δ | Δ% | status |
|---|---|---|---|---|---|---|
| arc_easy | acc | | | | | |
| hellaswag | acc_norm | | | | | |
| ifeval_subset | prompt_acc | | | | | |

## Interpretation

- <Which capabilities held, which moved.>
- If any suite regressed > bound: candidate mitigations —
  fewer epochs, lower LoRA rank, smaller learning rate, add a small slice of
  general instruction data to the training mix.

## Decision

<Keep current adapter / retrain with adjusted config — state which and why.>
